<UserControl x:Class="YfmCompanion.Desktop.Controls.CardPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="190" Margin="0,0,0,8">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="InputLabel" FontSize="12" Foreground="{StaticResource MutedBrush}" Margin="1,0,0,3" />
        <TextBox x:Name="InputBox"
                 Grid.Row="1"
                 TextChanged="InputBox_TextChanged"
                 GotKeyboardFocus="InputBox_GotKeyboardFocus"
                 LostKeyboardFocus="InputBox_LostKeyboardFocus"
                 PreviewKeyDown="InputBox_PreviewKeyDown"
                 AutomationProperties.Name="Card name" />
        <Popup x:Name="SuggestionPopup"
               Grid.Row="1"
               PlacementTarget="{Binding ElementName=InputBox}"
               Placement="Bottom"
               StaysOpen="False"
               AllowsTransparency="True"
               PopupAnimation="Fade">
            <Border Background="#172C3D" BorderBrush="#55C5E8" BorderThickness="1" Width="330" MaxHeight="320" Padding="4">
                <ListBox x:Name="SuggestionList"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{StaticResource TextBrush}"
                         PreviewMouseLeftButtonUp="SuggestionList_PreviewMouseLeftButtonUp">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="48" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="82" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Id, StringFormat=\#{0:000}}" Foreground="{StaticResource GoldBrush}" />
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding PrimaryType}" FontSize="12" Foreground="{StaticResource MutedBrush}" />
                                </StackPanel>
                                <TextBlock Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Run Text="{Binding Attack}" /><Run Text=" / " /><Run Text="{Binding Defense}" />
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Popup>
    </Grid>
</UserControl>
